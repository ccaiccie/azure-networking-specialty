name: Validate Azure Daily Study Report
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validation notice
        run: |
          echo "Per-run publisher performs the full precommit and fetch-back validation."
          echo "This legacy workflow is intentionally read-only and no longer rewrites report content."
