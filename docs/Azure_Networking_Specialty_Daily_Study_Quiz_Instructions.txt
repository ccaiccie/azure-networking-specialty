AZURE NETWORKING SPECIALTY — DAILY INTERACTIVE STUDY REPORT INSTRUCTIONS
Version: 1.2
Repository source of truth: ccaiccie/azure-networking-specialty, branch main
Publication directory: docs/
Pages base URL: https://ccaiccie.github.io/azure-networking-specialty/
Delivery recipients: mikewissa@yahoo.com; mwissa@osidigital.com
Schedule: daily at 08:00 America/Los_Angeles

PURPOSE
Create a rigorous, blueprint-based Azure networking professional-development report. Do not claim that an official certification named "Azure Networking Specialty" is active. When certification alignment is relevant, describe the material as Azure networking professional development aligned to the current Microsoft Azure networking role/skills outline, and verify any certification-status claim from an official Microsoft page.

PREFLIGHT — REQUIRED BEFORE RESEARCH
1. Fetch this instruction file from main and obey it.
2. Fetch docs/azure-study-uniqueness-ledger.json from main.
3. Enforce a rolling 30-calendar-day blocklist. A candidate is prohibited if any primary article URL, scenario article URL, canonical URL, normalized title/topic fingerprint, feature lesson, or source-case fingerprint matches an in-window ledger entry.
4. Record the preflight timestamp and active blocked fingerprints in validation. Do not update the ledger until all validation and fetch-back checks pass.

CONTENT REQUIREMENTS
- Create one timestamped self-contained interactive HTML report in docs/ named Azure_Networking_Specialty_Daily_Study_Quiz_YYYY-MM-DD-HH-MM.html.
- Include exactly 12 substantive Azure target lessons. Rotate lessons and source cases across days while preserving the 30-day blocklist.
- Every lesson MUST be a full self-contained mini-article, not a summary, outline, or bullet dump.
- HARD DEPTH GATE: each lesson body must contain at least 700 substantive instructional words, excluding headings, citations, code blocks, quiz text, navigation, and boilerplate. Target 800–1,200 substantive words when the topic supports it. Fail the run if any lesson is below 700 substantive words.
- A reader must be able to learn the feature without leaving the page. Source links remain required for verification, deeper reading, and attribution.
- Every lesson must include, when relevant: why it matters; architecture with control/data-plane roles; packet/traffic flow; prerequisites/dependencies; implementation/configuration using Portal plus Azure CLI/PowerShell/Bicep/Terraform examples where appropriate; routing/DNS/NAT/security/inspection/HA interactions; tradeoffs and alternatives; documented limitations/SKUs/scale/region/zone/protocol considerations; realistic enterprise scenario; troubleshooting and verification commands/checks/logs/metrics; common failure modes; exam/interview takeaways; concise summary.
- Use authoritative Microsoft sources (learn.microsoft.com, microsoft.com, Azure Architecture Center). Every lesson requires a primary source and a distinct scenario/case source; track them separately.
- Include verified official Microsoft/Azure-hosted figures when useful. Do not use unverified third-party figures.

DRAW.IO + SVG DIAGRAM REQUIREMENTS — HARD GATE
- For EVERY lesson where architecture, topology, packet flow, routing, DNS, NAT, security inspection, failover, private access, load balancing, observability flow, or service relationships can be visualized, create an original editable draw.io diagram. For this networking report, normally all 12 lessons should have a diagram.
- Store editable draw.io XML source files under `docs/images/` using a deterministic lesson-specific filename such as `YYYY-MM-DD-HH-MM-L01-topic.drawio`.
- Use Azure-specific objects/icons from the diagrams.net/draw.io Azure stencil libraries (prefer modern Azure/azure2 constructs). Do not use plain generic boxes when a relevant Azure service stencil/icon exists. Use correct Azure constructs such as ExpressRoute, VPN Gateway, Virtual WAN, Virtual Network, Load Balancer, Azure DNS, Front Door, Azure Firewall, Network Watcher, Private Link, Private Endpoint, Storage/Log Analytics, and other appropriate Azure resources.
- Diagrams must be colorful, professionally laid out, readable, and teaching-oriented. Use Azure blue plus distinct colors for trust zones/flows. Use containers for VNets/subnets/regions where relevant. Label important traffic flows, protocols, ports, BGP/IPsec/DNS/TLS stages, or control-plane relationships. Avoid clutter and crossing connectors.
- Export/convert every draw.io diagram to SVG and store the SVG beside the source in `docs/images/` with the same base filename.
- The SVG must be directly viewable in a browser and preserve readable labels and flow direction. Do not rasterize the diagram into the SVG.
- Embed each lesson’s SVG in the HTML near the architecture/traffic-flow explanation using a relative `images/<filename>.svg` reference and meaningful alt text/caption. The report must remain understandable if Mermaid is unavailable.
- Keep Mermaid teaching diagrams as a complementary representation when useful; draw.io/SVG does NOT replace the Mermaid requirement. Prefer at least one Mermaid block per lesson plus the draw.io/SVG diagram.
- Validate every referenced draw.io and SVG asset after commit: both files exist, SVG returns valid `<svg>` content, HTML references the correct relative path, and each diagram is associated with the intended lesson.
- Fail validation if a relevant lesson lacks its draw.io source, SVG export, or embedded HTML reference, or if the diagram uses only generic shapes where Azure stencil objects are available.

TOPIC COVERAGE FOR EACH DAILY REPORT
The 12 lessons must collectively cover:
1. Hybrid connectivity and architecture: Virtual WAN, hub/spoke transit, VPN Gateway, ExpressRoute, FastPath, Route Server, BGP.
2. Core networking infrastructure: VNets, subnets, IP addressing, DNS, Standard Load Balancer, Application Gateway, Front Door/Traffic Manager as appropriate.
3. Routing and traffic management: system routes, UDRs, BGP propagation, routing intent, effective routes, load-balancing/traffic-steering decisions.
4. Security, monitoring, and private service access: NSGs/ASGs, Azure Firewall, WAF, DDoS Protection, Private Link/private endpoints, Azure DNS private resolution, Network Watcher/Connection Monitor.

QUIZ REQUIREMENTS
- Exactly 50 automatically gradable single-answer questions, four choices each, no ambiguous answer.
- Required distribution: 15 Hybrid connectivity and architecture; 13 Core networking infrastructure; 10 Routing and traffic management; 12 Security, monitoring, and private service access.
- Every question needs correct answer, explanation, domain tag, associated lesson, and source link. Randomize option order deterministically while preserving correct-answer mapping.
- Provide Check answer feedback for every question; Finish and grade; score; percentage; domain breakdown; unanswered count; answer review; reset. No server-side component.

PRECOMMIT VALIDATION
Before publication, record and validate: 12 lessons; each lesson >=700 substantive words; >=12 Mermaid blocks with at least one per lesson when applicable; 12 distinct primary/scenario pairs; 30-day uniqueness; required draw.io/SVG assets; valid SVG markup; HTML image references; exactly 50 questions with 15/13/10/12 distribution and working quiz controls. If any check fails, stop without publishing, updating index, advancing ledger, or emailing.

PUBLICATION AND FETCH-BACK VALIDATION
1. Commit report and all `docs/images/` diagram source/export files to main.
2. Update docs/index.html so the latest validated report is prominent and uses exact Pages URL https://ccaiccie.github.io/azure-networking-specialty/<filename>.html.
3. Fetch committed report, index, ledger, and diagram assets back from GitHub.
4. Revalidate exact path/index URL; 12 deep lessons; per-lesson word counts; architecture/scenario/troubleshooting coverage; 50 questions and 15/13/10/12 distribution; source-role separation; quiz controls/final grading; verified official figures; Mermaid; draw.io/SVG asset pairs and HTML references; and 30-day uniqueness.
5. Advance the uniqueness ledger only after report/index/assets fetch-back validation succeeds; fetch the updated ledger back and confirm the run exists.
6. If any validation fails, do not email and do not advance the ledger.

EMAIL AFTER SUCCESS ONLY
Use connected Gmail to send an HTML email to both recipients.
Subject: Azure Networking Specialty Daily Study Quiz — YYYY-MM-DD-HH-MM
Include exact clickable + visible Pages URL, 12 short topic summaries, per-lesson word counts, validation results including article-depth and diagram validation, number of draw.io/SVG diagrams, and 30-day uniqueness status. Confirm Gmail reports SENT before stating success.
